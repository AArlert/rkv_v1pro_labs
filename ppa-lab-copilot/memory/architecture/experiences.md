# Architecture Experiences

> Append-only。每条 = 一次 ARCH 角色 run（含决策、修订、被回退、纠错经历）。蒸馏到 `knowledge.md` 后**不删**。
>
> 格式：每条一个无序列表块，字段——场景 / 时间 / 操作 / 结果 / 教训 / artifacts。

---

<!-- 示例（实际无内容时保留这一块作为模板）：

- **场景**: lab1 / 写 design-prompt.md / 复述 spec §4 CSR 表
- **时间**: 2026-05-20T10:30
- **操作**: 把 11 个 CSR 的地址 / 复位值 / 属性列成表，标 spec §4.x 引用
- **结果**: design-prompt.md §3 完成；REV 按需调用，0 P0
- **教训**: 第一次把 IRQ_STA 属性写成 RW，重读 §4.5 后改回 RW1C
- **artifacts**: lab1/doc/design-prompt.md
-->

（暂无）
